NODE_ENV=development
HOST=127.0.0.1
PORT=8080

CORS_ORIGIN=http://127.0.0.1:8081,http://localhost:8081,http://127.0.0.1:8080

DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=demo
DB_NAME=kwl_lavanderia

AUTH_TOKEN_SECRET=kwl-backend-super-secret-2024

# Cámara 1 (control PTZ/audio/relés)
CAMERA_BASE_URL=http://camera1.local
CAMERA_USER=admin
CAMERA_PASS=admin
# Cámara 1 (stream). Si no se define, usa CAMERA_USER/CAMERA_PASS
CAMERA_STREAM_USER=
CAMERA_STREAM_PASS=

# Cámara 2 (segunda tienda)
CAMERA2_BASE_URL=http://camera2.local
CAMERA2_USER=admin
CAMERA2_PASS=admin
# Cámara 2 (stream). Si no se define, usa CAMERA2_USER/CAMERA2_PASS
CAMERA2_STREAM_USER=
CAMERA2_STREAM_PASS=

MQTT_URL=mqtt://127.0.0.1:1883
MQTT_USER=
MQTT_PASS=
MQTT_ENABLED=true
