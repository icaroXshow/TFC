PORT=8080
HOST=0.0.0.0
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:8081,http://localhost

# MariaDB
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=
DB_PASSWORD=
DB_NAME=

# Auth (token)
AUTH_TOKEN_SECRET=dev-secret-change-me

# Cámara (MOBOTIX) - proxy backend
CAMERA_BASE_URL=
CAMERA_USER=
CAMERA_PASS=
